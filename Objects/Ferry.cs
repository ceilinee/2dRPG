using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//ferry boat controller
public class Ferry : Interactable {
    // Start is called before the first frame update
    void Start() {

    }

    // Update is called once per frame
    void Update() {

    }
}
